apiVersion: apps/v1
kind: Deployment
metadata:
  name: temporal-worker
  labels:
    app: temporal-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: temporal-worker
  template:
    metadata:
      labels:
        app: temporal-worker
    spec:
      imagePullSecrets:
        - name: ecr-credentials
      containers:
        - name: temporal-worker
          # Placeholder — overwritten by CI: kubectl set image deployment/temporal-worker temporal-worker=<ECR_URL>:<sha>
          image: <ECR_REPOSITORY_URL>:latest
          command: ["python", "-m", "interactors.temporal.worker"]
          env:
            - name: PYTHONPATH
              value: /app/src
            - name: TEMPORAL_HOST
              value: "temporal:7233"
            - name: AWS_DEFAULT_REGION
              value: us-east-1
            - name: AWS_S3_BUCKET
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: aws-s3-bucket
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: aws-access-key-id
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: aws-secret-access-key
            - name: KAGGLE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: kaggle-username
                  optional: true
            - name: KAGGLE_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: kaggle-key
                  optional: true
            - name: KAGGLE_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: kaggle-key
                  optional: true
            - name: RUNPOD_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: runpod-api-key
                  optional: true
            - name: VAST_API_KEY
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: vast-api-key
                  optional: true
            - name: REMOTE_HOST
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: remote-host
                  optional: true
            - name: REMOTE_USER
              valueFrom:
                secretKeyRef:
                  name: llm-api-secrets
                  key: remote-user
                  optional: true
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: llm-api-db-secret
                  key: database-url
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "2Gi"
